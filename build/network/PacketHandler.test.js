"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/*const packetHandler = new PacketHandler("Server");
test('should have id', () => {
    const data = [0,0x12]
    let buffer = new Buffer(data)
    console.log(buffer.toString())

    const result =packetHandler.processChunk(buffer)
    console.log(result)

    //expect(result.packetId).toEqual(0)
})
*/ 
